# Plan

Nothing in flight. This file held the root-cause triage for the first
faults.md batch; all nine items landed (the last being the span/edge renderer
port, `quake/r_edge.py`). The writeups live in the git history and the
regression tests that pin each fix.
