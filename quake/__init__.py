"""pq.ai — a slice of Quake in pure Python. Platform-agnostic engine package.

UI (tkinter) and platform audio backends live outside this package, at the repo
root (main.py, mac.py), so the engine itself imports nothing platform-specific.
"""
